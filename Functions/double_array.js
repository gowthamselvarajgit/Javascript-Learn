const processArray = (arr,times) => arr.map(times);

const double = (num) => num * 2;
const trible = (num) => num * 3;
const fours = (num) => num * 4;
const fives = (num) => num * 5;

console.log(processArray([1,2,3], trible));